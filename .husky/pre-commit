#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run catalog validation before committing
echo "🔍 Validating catalog files..."
node scripts/validate-catalog.js

# Run lint-staged for code formatting
npx lint-staged
